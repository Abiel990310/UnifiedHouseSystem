# RuView Hub — Raspberry Pi Zero 2 W dependencies
#
# numpy and scipy are intentionally excluded here — they are installed
# via apt (python3-numpy, python3-scipy) and accessed through the
# --system-site-packages venv to avoid compiling from source on the Pi.

# API framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# MQTT
aiomqtt==2.3.0

# Configuration
pyyaml==6.0.2
pydantic==2.9.2
pydantic-settings==2.6.1

# Storage
aiosqlite==0.20.0

# WebSocket support (included in uvicorn[standard] via websockets)
websockets==13.1

# Utilities
python-multipart==0.0.12
